# Aether Application SDK

The SDK will contain only interfaces intended for applications.

The SDK is currently a specification workspace; no stable application ABI is claimed yet.

When an interface is implemented in Aether OS, its contract should be documented here before applications begin depending on it.
