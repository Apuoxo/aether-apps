# Applications

Native Aether OS applications live here.

Each application should normally have its own directory and README describing:

- purpose;
- current implementation status;
- required capabilities;
- Aether OS interface dependencies;
- runtime test status.

The first application will be the Calculator, once its userspace application interface is established.
